<%= bootstrap_form_with(model: grant, local: true) do |form| %>
  <% if grant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grant.errors.count, "error") %> prohibited this grant from being saved:</h2>

      <ul>
      <% grant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.select :project_id, Project.all.map {|p| [p.id.split(':')[-1], p.id] }, { include_blank: true } %>
  <%= form.select :resource_id, Database.all.map {|d| [d.id.split(':')[-1], d.id] }, { include_blank: true } %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
